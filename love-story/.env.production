# URL backend production (Render). Đây là giá trị dự đoán dựa trên tên
# service `lovestory-backend` khai báo trong render.yaml — nếu Render báo
# tên bị trùng và tự đổi thành tên khác, sửa dòng dưới cho khớp URL thật
# trong Render dashboard → Settings → URL.
VITE_API_URL=https://lovestory-backend.onrender.com
